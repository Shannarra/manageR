<%= form_with(model: i_class, url: classes_path(institution_id: current_user.institution.id)) do |form| %>
  <% if i_class&.errors&.any? %>
    <div style="color: red">
      <h2><%= pluralize(i_class.errors.count, "error") %> prohibited this i_class from being saved:</h2>

      <ul>
        <% i_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
  </div>

  <div>
    <%= form.label :institution_id, style: "display: block" %>
    <%= form.text_field :institution_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
